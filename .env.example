# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB
MONGODB_URI=mongodb://localhost:27017/disaster-relief

# For MongoDB Atlas (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/disaster-relief?retryWrites=true&w=majority

# API Configuration
API_VERSION=v1

# CORS Configuration (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000

# Security (optional - for future auth implementation)
# JWT_SECRET=your_jwt_secret_key_here
# JWT_EXPIRE=7d